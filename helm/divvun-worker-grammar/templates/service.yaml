apiVersion: v1
kind: Service
metadata:
  name: {{ include "divvun-worker-grammar.fullname" . }}
  labels:
    {{- include "divvun-worker-grammar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "divvun-worker-grammar.selectorLabels" . | nindent 4 }}